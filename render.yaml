services:
  - type: cron
    name: mapit-mantenimiento-v4
    env: python
    plan: free
    schedule: "0 22 * * *"
    buildCommand: "pip install -r requirements.txt"
    startCommand: "python mapit_mantenimiento.py importar-gmail --ntfy"
