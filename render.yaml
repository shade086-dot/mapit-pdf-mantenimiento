services:
  - type: cron
    name: mapit-pdf-mantenimiento
    env: python
    plan: starter
    schedule: "0 22 * * *"
    buildCommand: "pip install -r requirements.txt"
    startCommand: "python mapit_mantenimiento.py importar-drive --ntfy"
