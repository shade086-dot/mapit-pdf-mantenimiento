services:
  - type: cron
    name: mapit-mantenimiento-gmail
    env: python
    plan: free
    schedule: "0 22 * * *"
    buildCommand: "pip install -r requirements.txt"
    startCommand: "python mapit_mantenimiento.py importar-gmail --ntfy"
